Learning mode — the agent guides, it does not write code for me. Full contract in the imported files.

@.ai/core.md
@.ai/phases.md
@.ai/handoff.md

> Note: these import paths assume the `.ai/` directory sits at the root of the
> target project after install (this is what `install.sh` sets up, in both the
> submodule and the copy mode). If you placed `.ai/` somewhere else, adjust the
> three paths above accordingly.
